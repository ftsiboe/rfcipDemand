% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fcip_demand_sys_estimate.R
\name{fcip_demand_elasticities_calibrate}
\alias{fcip_demand_elasticities_calibrate}
\title{Calibrate FCIP demand elasticities via IV-SEM (lavaan)}
\usage{
fcip_demand_elasticities_calibrate(
  data,
  estimator = c("ML", "MLR"),
  missing = c("fiml", "listwise")
)
}
\arguments{
\item{data}{data.frame with cols: instr_rate, tilda_rate, tilda_Gamma, tilda_Theta1}

\item{estimator}{"ML" or "MLR" (default "MLR" = robust SE/tests)}

\item{missing}{"fiml" or "listwise" (default "fiml")}
}
\value{
A list with:
\itemize{
\item \code{fit}: the lavaan fit object
\item \code{pe}:  parameter estimates (including standardized if requested)
\item \code{rsq}: R-squareds (first-stage strength)
\item \code{converged}: logical convergence flag
}
}
\description{
Fits a just-identified IV-style SEM where \code{instr_rate} instruments \code{tilda_rate},
and \code{tilda_rate} enters two outcome equations (\code{tilda_Gamma}, \code{tilda_Theta1}).
Endogeneity is encoded via residual correlations between \code{tilda_rate} and each outcome.
The instrument is excluded from the outcome disturbances.
}
\details{
Constraints imposed:
\enumerate{
\item b1 < 0,  2) b2 < 0,  3) b1 + b2 + b1*b2 < 0
}
}
