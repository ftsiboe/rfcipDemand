% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fcip_demand_data_dispatcher.R
\name{fcip_demand_data_dispatcher}
\alias{fcip_demand_data_dispatcher}
\title{Build dataset to estimate Federal Crop Insurance Program (FCIP) demand (modular pipeline)}
\usage{
fcip_demand_data_dispatcher(
  study_years = 2001:(as.numeric(format(Sys.Date(), "\%Y")) - 1),
  identifiers = c("commodity_year", FCIP_INSURANCE_POOL, "insurance_plan_code",
    "unit_structure_code")
)
}
\arguments{
\item{study_years}{Integer vector of commodity years to include.
Defaults to \code{2001:(as.numeric(format(Sys.Date(), "\%Y")) - 1)}.}

\item{identifiers}{Character vector of grouping keys that define the aggregation grain.
Must be columns present in SOBTPU (e.g., \code{"commodity_year"}, \code{FCIP_INSURANCE_POOL},
\code{"insurance_plan_code"}, \code{"unit_structure_code"}, and-if desired-additional keys like
\code{"commodity_code"} or \code{"practice_code"}). Enrichment joins for recodes are performed
only when the required keys are included in \code{identifiers}}
}
\value{
A \code{data.table} ready for FCIP demand estimation.
}
\description{
End-to-end pipeline that:
(1) prepares SOBTPU and coverage aggregates,
(2) adds prices/instruments/rental rates/price index,
(3) reconciles county acreage (FSA + NASS), and
(4) finalizes bins/labels/pooling for demand estimation.
}
\details{
Aligned with Asche, Bekkerman, & Li (2023), \emph{Food Policy}, 119(3):102505
(\doi{10.1016/j.foodpol.2023.102505}). Requires internet access to download
release \code{.rds} assets and several in-memory lookup tables (see stage docs).
}
\concept{Estimation Data}
