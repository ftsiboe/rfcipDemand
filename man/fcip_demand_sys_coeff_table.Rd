% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fcip_demand_sys_estimate.R
\name{fcip_demand_sys_coeff_table}
\alias{fcip_demand_sys_coeff_table}
\title{Tidy coefficient table with cluster-robust SEs (from supplied VCOV)}
\usage{
fcip_demand_sys_coeff_table(fit, vcMat, p_digits = 5)
}
\arguments{
\item{fit}{A fitted \code{systemfit} object.}

\item{vcMat}{A covariance matrix conformable with \code{coef(fit)}. Row/column
names are used to align; if missing, positional alignment is assumed.}

\item{p_digits}{Integer; number of digits to keep for p-values (default \code{5}).}
}
\value{
A \code{data.frame} with columns:
\code{demand}, \code{coef}, \code{Estimate}, \code{StdError}, \code{Zvalue}, \code{Pvalue}.
}
\description{
Builds a clean coefficient table for a \code{systemfit} model using a \strong{user-supplied
covariance matrix} (e.g., two-way clustered from \code{fcip_demand_sys_vcov()}).
Estimates come from \code{coef(fit)}, standard errors from \code{diag(vcMat)}, then
Z-scores and two-sided normal p-values are computed. The \code{demand} column is
inferred from the equation prefix in the coefficient names:
\itemize{
\item \code{"Gamma_*"} to \code{"Gamma"}
\item \code{"Theta_*"}, \code{"Theta1_*"}, \code{"Theta2_*"}, ... to \code{"Theta"}
Otherwise the prefix itself is used.
}
}
