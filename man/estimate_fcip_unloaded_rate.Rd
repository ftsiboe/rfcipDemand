% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/estimate_fcip_unloaded_rate.R
\name{estimate_fcip_unloaded_rate}
\alias{estimate_fcip_unloaded_rate}
\title{Estimate FCIP Unloaded (County) Rates}
\usage{
estimate_fcip_unloaded_rate(
  statplan,
  year = 2011,
  crop = NULL,
  state = NULL,
  county = NULL
)
}
\arguments{
\item{statplan}{A \link[data.table]{data.table} containing FCIP rate elements
with at least the columns:
\describe{
\item{state_code, county_code}{Identifiers for each county.}
\item{contiguous_state_code, contiguous_county_code}{Mapping to county group.}
\item{insured_area}{Total insured acres in the county.}
\item{lcr}{Loss Cost Rate for each county.}
\item{commodity_code}{Crop identifier.}
}}

\item{year}{Integer. Crop year for which rates are being estimated
(currently not used but reserved for future subsetting).}

\item{crop}{Optional vector of commodity codes to filter by crop.}

\item{state}{Optional vector of state codes to restrict the analysis.}

\item{county}{Optional vector of county codes to restrict the analysis.}
}
\value{
A \code{data.frame} with columns:
\describe{
\item{state_code, county_code, commodity_code}{Keys identifying county and crop.}
\item{tau}{Estimated FCIP county unloaded rate.}
}
}
\description{
Computes the unloaded loss cost rates (\code{tau}) for counties based on
the FCIC Rate Methodology Handbook (2009), pp. 65-70.
}
\details{
\enumerate{
\item \strong{Target data} is filtered to the selected state(s)/county(ies).
\item \strong{Group data} finds contiguous-county groupings, unions them with the target.
\item Computes group-level statistics:
}
\itemize{
\item \code{c_alpha}: mean insured acres
\item \code{c_u}: mean LCR
\item \code{c_a}: variance of LCR
}
\enumerate{
\item Computes target county statistics:
}
\itemize{
\item \code{c_x}: mean LCR
\item \code{c_v}: variance of LCR
\item \code{c_net_acre}: total insured acres
}
\enumerate{
\item Applies the blending formula
\deqn{\tau = Z\,x + (1 - Z)\,u, \quad Z = P/(P + K)}
where
\eqn{P = c_{\!net\_acre}/c_\alpha,\quad K = c_v/c_a.}
}
}
\references{
FCIC Rate Methodology Handbook APH (2009), pp. 65-70.
\url{https://legacy.rma.usda.gov/pubs/2008/ratemethodology.pdf}
}
\concept{FCIP rating helpers}
